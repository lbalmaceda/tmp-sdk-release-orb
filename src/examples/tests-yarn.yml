description: >
  Runs the tests using yarn

usage:
  version: 2.1
  orbs:
    node-publisher: lbalmaceda/node-publisher@0.0.1
  workflows:
    build-and-test:
      jobs:
        - node-publisher/test:
            pkg-manager: yarn
